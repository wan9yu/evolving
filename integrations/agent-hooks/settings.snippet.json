{
  "_comment": "Merge into your agent runtime's hook settings file. Replace ABSOLUTE_PATH with where this repo lives. The session-start hook auto-injects `ev brief` so a fresh agent heeds the settled decisions without being asked to load them. (This shape matches runtimes that load hooks from a JSON settings file under the project; a SessionStart-style event runs the command and adds its stdout to the session.)",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh ABSOLUTE_PATH/integrations/agent-hooks/ev-brief-sessionstart.sh"
          }
        ]
      }
    ]
  }
}
